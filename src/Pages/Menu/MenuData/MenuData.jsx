import { MenuItem } from "@mui/material";
import useMenu from "../../../hooks/useMenu";

const MenuData = () => {
    return (
        <div>
            
        </div>
    );
};

export default MenuData;